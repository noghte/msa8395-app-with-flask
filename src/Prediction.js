function Prediction({algorithm, prediction}) {

    return(
        <div>
            <p>Algorithm:</p> {algorithm}
            <br />
            <p>Prediction:</p> {prediction}
        </div>
    )
}
export default Prediction;